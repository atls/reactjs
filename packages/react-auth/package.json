{
  "name": "@atls/react-auth",
  "version": "0.1.42",
  "license": "BSD 3-Clause",
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "exclude-tests": "rm -rf dist/unit && cp -R dist/src/ dist/ && rm -rf dist/src",
    "build": "yarn app library build && yarn exclude-tests",
    "postpack": "rm -rf dist",
    "prepack": "yarn run build"
  },
  "peerDependencies": {
    "react": "16.14.0"
  },
  "devDependencies": {
    "@atlantis-lab/jest-config": "^0.1.2",
    "@types/jest": "^26.0.20",
    "@types/react": "^17.0.20",
    "react": "^17.0.2",
    "react-test-renderer": "^17.0.1",
    "ts-jest": "^26.4.4",
    "typescript": "4.2.3"
  },
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "typings": "dist/index.d.ts"
  }
}
